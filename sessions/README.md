# Sessions

> One recap file per play session — the campaign's running memory. The
> **session-chronicler** writes these at the end of each session as
> `session-NN-<short-title>.md`.

Each recap holds:
- A short **"Previously…"** paragraph (1–2 paras) to open the next session.
- **Key events** (bulleted).
- **State changes** — reputation, NPCs, locations, inventory, character growth.
- **Open threads / hooks** left dangling, for the DM to pick up.

*(No sessions played yet. Session 1 begins after character creation —
`character/CHARACTER-CREATION.md`.)*
